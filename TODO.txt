- HttpTask:
    - Tests: JsonKeysFinder
        data extraction from simple/nested values and arrays
    - Tests: HttpTask:
        success
        error
        timeout
        caching responses for the trend display

- UI for HttpTask
- UI tests with espresso (special test dagger-component?)

? inject adapter: http://frogermcs.github.io/inject-everything-viewholder-and-dagger-2-example/

// Ping task: proper UI (item view) + calculate and display uptime and downtime
